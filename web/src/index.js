import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js';

ReactDOM.render(<App />, document.getElementById('root'));

//JSX (JavaScript + XML) O XML é a sintaxe do HTML. O REACT é importado quando o HML é usado no código
//renderizando o App no Root ou seja está como que enjetando
